---
---

<section id="solucion" class="solution section">
  <div class="container">
    <h2 class="section-title text-center">
      La solución integral que no sabías que necesitabas
    </h2>
    <div class="solution-content">
      <p class="solution-subtitle text-center">
        Creamos el <strong>primer ecosistema especializado</strong> para desarrolladoras inmobiliarias: un equipo de equipos que trabaja en sincronía para que cada parte del proceso funcione sin fricciones.
      </p>
      <div class="solution-benefits">
        <div class="benefit-card">
          <div class="benefit-icon">🚀</div>
          <h3>Velocidad</h3>
          <p>Reducimos el tiempo hasta la preventa en 80%</p>
        </div>
        <div class="benefit-card">
          <div class="benefit-icon">💎</div>
          <h3>Valor</h3>
          <p>Aumentamos el valor percibido por m²</p>
        </div>
        <div class="benefit-card">
          <div class="benefit-icon">🧠</div>
          <h3>Eficiencia</h3>
          <p>Eficiencia operativa sin perder el control</p>
        </div>
        <div class="benefit-card">
          <div class="benefit-icon">🎯</div>
          <h3>Impacto</h3>
          <p>Comunicación visual de alto impacto</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Estructura y fondo */
  .solution {
    background: linear-gradient(120deg, #f8faff 0%, #fff 100%);
    position: relative;
    border-bottom: 1px solid var(--color-border);
  }
  
  .solution::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(circle at 10% 10%, rgba(var(--color-secondary-rgb, 109, 40, 217), 0.05) 0%, transparent 60%);
    pointer-events: none;
  }
  
  /* Título y subtítulo */
  .section-title {
    margin-bottom: var(--space-lg);
  }
  
  .solution-content {
    position: relative;
    z-index: 1;
  }
  
  .solution-subtitle {
    font-size: clamp(1.125rem, 2vw, 1.5rem);
    color: var(--color-text-light);
    margin-bottom: var(--space-xl);
    max-width: 900px;
    margin-inline: auto;
  }
  
  .solution-subtitle strong {
    color: var(--color-primary);
    font-weight: 700;
  }
  
  /* Beneficios */
  .solution-benefits {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-lg);
  }
  
  .benefit-card {
    padding: var(--space-xl);
    background: white;
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-md);
    transition: var(--transition-default);
    text-align: center;
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
  }
  
  .benefit-card:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-lg);
    border: 1px solid var(--color-primary);
  }
  
  .benefit-icon {
    font-size: 3rem;
    margin-bottom: var(--space-md);
  }
  
  .benefit-card h3 {
    font-size: clamp(1.25rem, 2vw, 1.75rem);
    margin-bottom: var(--space-sm);
    color: var(--color-text);
  }
  
  .benefit-card p {
    color: var(--color-text-light);
    margin-bottom: 0;
  }
  
  /* Media Queries */
  @media (min-width: 576px) {
    .section-title {
      margin-bottom: var(--space-xl);
    }
    
    .solution-benefits {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  
  @media (min-width: 768px) {
    .solution-subtitle {
      margin-bottom: var(--space-xxl);
    }
  }
  
  @media (min-width: 1024px) {
    .solution-benefits {
      grid-template-columns: repeat(4, 1fr);
      gap: var(--space-xl);
    }
    
    .benefit-card h3 {
      margin-bottom: var(--space-md);
    }
  }
</style>
